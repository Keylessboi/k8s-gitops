# DEPRECATED: Will be replaced by central app-databases cluster in databases namespace
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: nextcloud-db
  namespace: nextcloud
spec:
  instances: 1
  postgresql:
    parameters:
      max_connections: "100"
  storage:
    size: 1Gi
