---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: doppler
  namespace: argocd
  labels:
    app.kubernetes.io/name: doppler
    app.kubernetes.io/part-of: homelab
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Keylessboi/k8s-gitops.git
    targetRevision: main
    path: apps/doppler
  destination:
    server: https://kubernetes.default.svc
    namespace: doppler
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
