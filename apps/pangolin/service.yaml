apiVersion: v1
kind: Service
metadata:
  name: pangolin
  namespace: pangolin
  labels:
    app.kubernetes.io/name: pangolin
    app.kubernetes.io/part-of: homelab
spec:
  selector:
    app.kubernetes.io/name: pangolin
  ports:
    - name: http
      port: 3001
      targetPort: http
