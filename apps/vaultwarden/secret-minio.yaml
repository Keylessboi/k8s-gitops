---
# Placeholder for MinIO credentials. Replace before first backup, or
# let the Doppler operator manage a real Secret with the same name.
apiVersion: v1
kind: Secret
metadata:
  name: minio-creds
  namespace: vaultwarden
  labels:
    app.kubernetes.io/name: vaultwarden
    app.kubernetes.io/part-of: homelab
stringData:
  ACCESS_KEY_ID: REPLACE_WITH_MINIO_ACCESS_KEY
  SECRET_ACCESS_KEY: REPLACE_WITH_MINIO_SECRET_KEY
