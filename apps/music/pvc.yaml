---
# Beets' library database and configuration.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beets-config
  namespace: music
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 5Gi
